define([
    "dojo/_base/declare"
], function (declare) {
    return declare([], {
        // summary:
        //      Добавляет колбеки модификации формы.
        initForm: function (form) {
        },

        updateFormAfterSaveNew: function (form) {
        }
    });
});